#include <stdio.h>
/**
 * main - Entry point
 * @argc: int
 * @argv: int
 * Return: Always 0 (Success)
 */
int main(int argc, char __attribute__((__unused__)) *argv[])
{
	(void) argc;
	printf("%s\n", argv[0]);
	return (0);
}
