#include "main.h"
#include <stdio.h>
/**
 * _strlen - Entry point
 * @s : int
 * Return: Always 0 (Success)
 */
int _strlen(char *s)
{
	int i, c;

	c = 0;
	for (i = 0; i < '\0'; i++)
	{
		s[i];
	}

	return (i);
}
